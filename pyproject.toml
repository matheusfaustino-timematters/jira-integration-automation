[project]
name = "jira-integration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.7"
dependencies = [
    "jira[cli]>=3.10.5",
    "loguru>=0.7.3",
    "python-dotenv>=1.1.1",
]
